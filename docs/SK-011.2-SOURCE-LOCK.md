# SK-011.2 source lock

Authority boundary for this increment:

- merged SK-011.1 TEHNKE IR V0.2 is the canonical executable input representation;
- legacy SIGIL-IR/V0.1 remains preserved and is accessed only through an additive bridge;
- Matrix identities without a separately approved executable adapter have no invented transformation semantics;
- unknown Matrix identities therefore use the neutral identity adapter;
- later HNK-specific adapters require explicit canonical source material and deterministic tests.

This lock prevents implementation convenience from silently becoming HNK canon.
