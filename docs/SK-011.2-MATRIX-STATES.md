# SK-011.2 Matrix states

`APPLIED` — the Matrix descriptor was consumed and materialized by the deterministic software execution layer.

`READY` — the manifestation-channel descriptor was materialized in the execution result and is available to downstream SIGILKODE components.

`IDENTITY` adapter — no canonical transformation adapter is registered for that Matrix identity; the descriptor passes through without invented transformation output.
