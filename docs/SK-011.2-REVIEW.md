# SK-011.2 review focus

Review specifically for:

1. accidental mutation of legacy V0.1 behavior;
2. loss of deterministic payload provenance;
3. Matrix order instability;
4. implicit semantics assigned to unknown Matrix identities;
5. adapter registry state leaking between executions/tests;
6. mismatch between runtime modules and TypeScript declarations.

Do not approve merge solely because files compile; repository-wide regression and Web build remain required gates.
