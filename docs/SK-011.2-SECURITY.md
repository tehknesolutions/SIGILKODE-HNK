# SK-011.2 execution safety contract

The Matrix adapter registry executes code only when an adapter was explicitly registered by application code. Matrix identity strings never resolve modules, filesystem paths, commands or network destinations. Unknown identities fall back to a neutral identity result. Adapter context is supplied explicitly by the caller.

Future adapters must preserve this rule: canonical identity is data, never executable code by itself.
